# Test Node Server

This repository contains a simple Node.js web server located in the `server` directory. It uses the built-in `http` module and listens on port `3000` by default.

## Running

```bash
cd server
npm start
```

Open [http://localhost:3000](http://localhost:3000) to see the greeting message.
